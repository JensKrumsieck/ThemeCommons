<controls:DefaultWindow x:Class="Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:ThemeCommons.Controls;assembly=ThemeCommons"
        mc:Ignorable="d"
        Background="{DynamicResource Light}"
        x:Name="TheWindow"
        Title="MainWindow" Height="450" Width="800">
    <controls:DefaultWindow.TitlebarContent>
        <Grid VerticalAlignment="Center">
            <Label Content="{Binding Title, ElementName=TheWindow}" FontSize="16" Foreground="{DynamicResource Light}" FontWeight="SemiBold"/>
        </Grid>
    </controls:DefaultWindow.TitlebarContent>
    <Grid> 
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="1" Style="{StaticResource ColorPickerTextBox}"/>

    </Grid>
</controls:DefaultWindow>
